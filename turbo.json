{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "typecheck": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src/**/*.ts",
        "test/**/*.ts",
        "bench/**/*.ts"
      ]
    },
    "test:ci": {
      "dependsOn": [
        "build",
        "typecheck"
      ],
      "inputs": [
        "src/**/*.ts",
        "test/**/*.ts"
      ]
    },
    "bench": {
      "cache": false,
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "src/**/*.ts",
        "bench/**/*.ts"
      ]
    },
    "format": {
      "inputs": [
        "src/**/*.ts",
        "test/**/*.ts",
        "bench/**/*.ts"
      ]
    },
    "dev": {
      "cache": false
    }
  }
}
